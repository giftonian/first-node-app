
//setInterval example

//The setInterval function schedules a function to run repeatedly, starting after the interval of time, 
// then repeating continuously at that interval. 
setInterval(() => { // Event loop will call setIterval after each 2000 miliseconds
    console.log("hello world")
}, 2000) // 

console.log("I will run first")
